#! /bin/bash
pkill chrome
[ -z ${TPW} ] && echo "No password set." && exit 1;
echo "Recommend licence test"

echo "Testing on env ${1}"

if [ "${1}" = "preprod" ]; then
   URL=${INT_PRE}
elif [ "${1}" = "dev" ]; then
   URL=${INT_DEV}
fi

./test_rl.pl ${2} ${TPW}  ${URL} text.txt ${3}

